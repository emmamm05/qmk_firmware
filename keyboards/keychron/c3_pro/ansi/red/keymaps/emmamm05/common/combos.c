#include "combos.h"
#include "keys.h"

// const uint16_t PROGMEM cb_left_ctl[]           = {KC_F, KC_SPC, COMBO_END};

combo_t key_combos[] = {
    // [CB_LCTL] = COMBO(cb_left_ctl, OSM(MOD_LCTL)),
};
